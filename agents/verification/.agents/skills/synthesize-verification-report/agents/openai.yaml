interface:
  display_name: "Synthesize Verification Report"
  short_description: "Build final report and strict verdict"
  default_prompt: "Use $synthesize-verification-report to aggregate findings and return the final Phase I verifier JSON."

policy:
  allow_implicit_invocation: true
