interface:
  display_name: "Check Referenced Statements"
  short_description: "Validate references against supplied verified nodes"
  default_prompt: "Use $check-referenced-statements to check each cited reference only against the supplied target and verified node context."

policy:
  allow_implicit_invocation: true
